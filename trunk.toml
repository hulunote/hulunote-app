[build]
target_dir = "dist"
public_url = "/"

[serve]
addresses = ["127.0.0.1"]
port = 8803
open = false

[watch]
features = []

# Tailwind is configured via index.html <link data-trunk rel="tailwind-css" ...>
